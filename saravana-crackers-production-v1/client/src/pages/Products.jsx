import{useEffect,useState}from"react";import{useParams}from"react-router-dom";import{api}from"../api";import ProductCard from"../components/ProductCard";
export default function Products(){const{category}=useParams();const[p,setP]=useState([]),[q,setQ]=useState("");useEffect(()=>{api(category?`/products?category=${category}`:"/products").then(setP)},[category]);const shown=p.filter(x=>x.name.toLowerCase().includes(q.toLowerCase())||x.category.name.toLowerCase().includes(q.toLowerCase()));return <section className="section"><div className="container"><div className="row"><h1>Products</h1><input className="input" placeholder="Search..." value={q} onChange={e=>setQ(e.target.value)}/></div><div className="products">{shown.map(x=><ProductCard key={x.id} p={x}/>)}</div></div></section>}
