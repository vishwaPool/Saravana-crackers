import{useState}from"react";import{useNavigate}from"react-router-dom";import{api}from"../../api";
export default function Login(){const[e,setE]=useState(""),[p,setP]=useState(""),[err,setErr]=useState("");const n=useNavigate();async function go(x){x.preventDefault();try{await api("/auth/login",{method:"POST",body:JSON.stringify({email:e,password:p})});n("/admin/dashboard")}catch(z){setErr(z.message)}}return <div className="login"><form className="panel form" onSubmit={go}><h1>Saravana Admin</h1>{err&&<div className="alert">{err}</div>}<input className="input" type="email" placeholder="Email" value={e} onChange={x=>setE(x.target.value)}/><input className="input" type="password" placeholder="Password" value={p} onChange={x=>setP(x.target.value)}/><button className="btn">Login</button></form></div>}
