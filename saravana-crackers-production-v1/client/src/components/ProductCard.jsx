import{useCart}from"../state/CartContext";
export default function ProductCard({p}){const{add}=useCart();const off=p.mrp>p.retailPrice?Math.round((p.mrp-p.retailPrice)/p.mrp*100):0;return <article className="product"><div className="pic">{p.imageUrl?<img src={p.imageUrl}/>:<span>🎆</span>}{off>0&&<b>{off}% OFF</b>}</div><div className="pad"><small>{p.category?.name}</small><h3>{p.name}</h3><p>{p.unit}</p><div className="price"><strong>₹{p.retailPrice.toLocaleString("en-IN")}</strong><del>₹{p.mrp.toLocaleString("en-IN")}</del></div><button disabled={p.stock<=0} className="btn" onClick={()=>add(p)}>{p.stock<=0?"Out of Stock":"Add to Cart"}</button></div></article>}
